module.exports = (client, guild, user) => {
  //let Log = client.guild.channels.find('name', 'lukki-log');
  //user.send("You were unbanned from " + guild + ".");
  //guild.Log.send(`${user.username} was just unbanned!`);
};
