module.exports = member => {
        // Send the message to a designated channel on a server:
        //const channel = member.guild.channels.find('name', 'lukki-log');
        // Do nothing if the channel wasn't found on this server
        //if (!channel) return;
        // Send the message, mentioning the member
        //channel.send(`Welcome to the server, ${member} We are now ${member.guild.memberCount} members in the server! Welcome and have a great time here if you need anything just contact a staff member and they will try to assist you with the issue you are having!`);
};
